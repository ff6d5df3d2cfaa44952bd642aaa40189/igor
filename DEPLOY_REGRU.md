# Деплой на REG.RU (shared-хостинг)

Сайт работает на shared-хостинге без Node.js и без сборки.

## Что загрузить на хостинг

Минимальный набор:

- `index.html`
- `catalog_chats_site_ready.json`

## Куда загрузить

В корневую папку сайта в ISPmanager:

`/www/<ваш-домен>/`

## Важно

- JSON должен лежать рядом с `index.html`.
- Формат файла — массив объектов, где используются поля вроде:
  - `ЖК`
  - `АО Москвы`
  - `Район / локация`
  - `Застройщик`
  - `Ссылка на чат`
- Если JSON не загрузится, сайт покажет тестовую запись.

## Проверка

1. Откройте `https://<ваш-домен>/`
2. Должны появиться карточки ЖК и фильтры.
3. Поиск и фильтры должны сразу работать.
