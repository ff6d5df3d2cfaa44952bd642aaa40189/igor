# Деплой на REG.RU (shared-хостинг)

Эта сборка повторяет исходный дизайн и функционал каталога (как в первоначальной версии),
но данные берутся из вашего файла базы ЖК.

## Что загрузить на хостинг

Обязательные файлы в одной папке:

- `index.html`
- `styles.css`
- `script.js`
- `catalog_chats_site_ready.json`

## Куда загрузить

В корневую папку сайта в ISPmanager:

`/www/<ваш-домен>/`

## Требования к JSON

Файл должен быть массивом объектов. Используются поля (с поддержкой вариантов):

- `ЖК` / `Название ЖК` / `Комплекс`
- `Район / локация` / `Район`
- `Класс` / `Тип списка`
- `Срок сдачи` / `Статус` / `Стадия`
- `Ссылка на чат` / `Ссылка исходная`

## Проверка

1. Откройте `https://<ваш-домен>/`
2. Должны появиться карточки ЖК и фильтры.
3. Если JSON не найден или поврежден — сайт покажет понятную ошибку загрузки.
